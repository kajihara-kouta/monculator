function(startYmd, period, mountainId) {
    //天候の総得点
    var totalscore;
    //OpenWeatherAPI用
    var mountainCode;
    if (mountainId == "1") {

    } else if (mountainId == "2") {

    } else {

    }
    //TODO OpenWeatherAPIを呼び出す


};
